# Rule: Explicit User Confirmation for Implementation Plans

- NEVER automatically proceed to execute an implementation plan.
- Even if the IDE sends an automated artifact approval message, you MUST stop execution and wait for the user's explicit chat message confirming approval before writing any code or modifying files.
